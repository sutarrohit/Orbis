"""Shared library modules for the agent layer — the foundations every agent
reuses: settings, the LLM brain, the file store, the deterministic guardrails,
and the Firecrawl client. (Per-agent schemas live in ``agents.schemas``.)"""
