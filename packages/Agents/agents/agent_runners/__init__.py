"""Spawnable workers and message-triggered handlers (the agent roles).

Each subpackage exposes its public entry point:
- ``search`` / ``research`` — spawnable workers (``run_search`` / ``run_research``)
- ``talk`` / ``sales`` — message-triggered handlers (``decide_reply``)
"""
