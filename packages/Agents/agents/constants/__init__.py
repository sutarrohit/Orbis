"""Constants for the agent layer — prompt templates and fixed values
(Telegram handle patterns, reserved usernames, default scores/queries) kept out
of the control-flow code so they can be tuned in one place."""
